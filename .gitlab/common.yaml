.before_build:
  before_script:
    - mkdir -p  ~/.docker
    - >
      echo -n "{
        \"auths\": {
          \"$CI_REGISTRY\": {
            \"auth\": \"$(echo -n $CI_REGISTRY_USER:$CI_REGISTRY_PASSWORD | base64 -w 0)\"
          }
        }
      }" > ~/.docker/config.json
